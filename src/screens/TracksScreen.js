import React, {useEffect, useRef} from 'react';
import {
  View,
  Text,
  StyleSheet,
  FlatList,
  TouchableOpacity,
  Button,
} from 'react-native';
// import Sound from 'react-native-sound';

const TracksScreen = ({}) => {
    return(
        <View><Text>SFDSFSFDSF</Text></View>
    )
};
const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    borderWidth: 1,
    
  }
});
export default TracksScreen;
